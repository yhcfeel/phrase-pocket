# GitHub Pages 版本验证记录

- 115 条初始词库，后续可追加新条目而不修改测试计数。
- 29 项自动检查：词库与保存、随机权重、断网资源、子路径、完整性和更新控制。
- 所有运行资源均在站点内，无外部字体、CDN 或 API 依赖。
- 新版下载不完整时不替换现有版本；点击更新后刷新，添加窗口打开时延迟刷新。
- 测试使用 Node.js 模拟 Service Worker、DOM 和本地存储；尚未在真实 iPhone Safari 上验证。
- 网络发布和 Pages 地址以实际部署结果为准。
